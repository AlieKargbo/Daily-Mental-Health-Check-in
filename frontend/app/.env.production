# This file is loaded automatically when you run 'npm run build' (which defaults to 'production' mode).

VITE_API_BASE_URL="https://apkargbo-wellshift-backend.hf.space"

// Corrected API Call:
// The call must include the full domain, not just /api/...
fetch(`${API_BASE_URL}/timeline`); // CORRECT